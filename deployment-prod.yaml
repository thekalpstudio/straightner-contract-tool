apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: ks-prod
  name: ks-contract-tool-backend-prod-deployment
  labels:
    app: ks-contract-tool-backend-prod
  # annotations:
  #   downscaler/exclude: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ks-contract-tool-backend-prod
  template:
    metadata:
      labels:
        app: ks-contract-tool-backend-prod
    spec:
      hostname: ks-contract-tool-backend-prod
      nodeSelector:
        noderole: ks-nonprod-node-x 
      containers:
        - name: ks-contract-tool-backend-prod
          image: 025066284787.dkr.ecr.ap-south-1.amazonaws.com/ks-contract-tool-backend-dev:<VERSION>
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: '600Mi'
              cpu: '150m'
            limits:
              memory: '600Mi'
              cpu: '150m'
          readinessProbe:
              httpGet:
                path: /health
                port: 3000
              initialDelaySeconds: 120
              timeoutSeconds: 30
          livenessProbe:
              httpGet:
                path: /health
                port: 3000
              initialDelaySeconds: 120
              periodSeconds: 5
              timeoutSeconds: 30
              failureThreshold: 5
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: PORT
              valueFrom:
                secretKeyRef:
                  key: PORT
                  name: prod-ks-straightner-contract-tool-backend
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  key: REDIS_URL
                  name: prod-ks-straightner-contract-tool-backend
            - name: QUEUE_NAME
              valueFrom:
                secretKeyRef:
                  key: QUEUE_NAME
                  name: prod-ks-straightner-contract-tool-backend
            - name: REMOVE_ON_COMPLETE
              valueFrom:
                secretKeyRef:
                  key: REMOVE_ON_COMPLETE
                  name: prod-ks-straightner-contract-tool-backend
            - name: REMOVE_ON_FAIL
              valueFrom:
                secretKeyRef:
                  key: REMOVE_ON_FAIL
                  name: prod-ks-straightner-contract-tool-backend
            - name: JOB_TTL_MS
              valueFrom:
                secretKeyRef:
                  key: JOB_TTL_MS
                  name: prod-ks-straightner-contract-tool-backend
            - name: WORKER_CONCURRENCY
              valueFrom:
                secretKeyRef:
                  key: WORKER_CONCURRENCY
                  name: prod-ks-straightner-contract-tool-backend
            - name: RATE_LIMIT_PER_MIN
              valueFrom:
                secretKeyRef:
                  key: RATE_LIMIT_PER_MIN
                  name: prod-ks-straightner-contract-tool-backend
---
apiVersion: v1
kind: Service
metadata:
  namespace: ks-prod
  name: ks-straightner-contract-tool-prod-svc
  labels:
    svc: ks-contract-tool-backend-prod
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
  selector:
    app: ks-contract-tool-backend-prod
